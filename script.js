
function add(x,y) {
    z = x + y;
    return z;
}

function subtract(x,y) {
    z = x - y;
    return z;
}

function multiply(x,y) {
    z = x * y;
    return z;
}

function divide(x,y) {
    z = x / y;
    return z;
}
function toThePowerOf(x,y) {
    z = x **y;
    return z;
}

function rootOf(x) {
    z = math.sqrt(x);
    return z;
}